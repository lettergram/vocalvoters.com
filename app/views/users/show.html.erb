<% provide(:title, @user.name) %>

<div class="row">
  <aside class="col-md-4">
    <section class="user_info">
      <h1>
        <%= @user.name %>, <%= @user.email %><br>
	<% if @user.organization.present? %>
	   Belongs to <%= @user.organization.name %>
	<% else %>
	  No Organizational Affiliation
	<% end %>
      </h1>
    </section>
  </aside>
</div>
